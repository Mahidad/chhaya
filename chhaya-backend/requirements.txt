fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
email-validator>=2.1

# auth
pyjwt>=2.8
passlib[bcrypt]>=1.7
bcrypt==4.0.1

# database — psycopg v3 with binary C extension + connection pool
psycopg[binary,pool]>=3.1

# reference source ingestion (Group 1 / Mahidad)
youtube-transcript-api>=0.6
# google-genai, NOT the retired google-generativeai: Google ended support for
# that package. All calls go through app/utils/gemini.py.
google-genai>=1.0
yt-dlp>=2024.1

# OCR is performed through OCR.Space's HTTP API using Python's standard library.
# No local Tesseract binary or OCR package is required.

# AI voice narration (Module 3 / Lamia)
edge-tts>=6.1

# word lookup (Module 2) + concept map extraction (Module 3 / Lamia)
# After installing, run once:
# python -m nltk.downloader punkt averaged_perceptron_tagger wordnet
nltk>=3.8

# practice problem bank import (scripts/import_practice_problems.py --kaggle)
kagglehub>=1.0
