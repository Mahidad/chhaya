fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
email-validator>=2.1

# auth
pyjwt>=2.8
passlib[bcrypt]>=1.7
bcrypt==4.0.1

# database — psycopg v3 with binary C extension + connection pool
# "binary" provides the compiled C extension for performance;
# "pool"   installs psycopg-pool (imported as psycopg_pool).
psycopg[binary,pool]>=3.1

# reference source ingestion (Group 1 / Mahidad)
youtube-transcript-api>=0.6
google-generativeai>=0.5
yt-dlp>=2024.1   # playlist enumeration - see app/utils/youtube_playlist.py

# ocr
pytesseract>=0.3
Pillow>=10.0
pdf2image>=1.17.0